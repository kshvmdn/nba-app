{
  "name": "nba-free-agent-tracker",
  "version": "0.0.0",
  "description": "",
  "repository": {
    "type": "git",
    "url": "https://github.com/kshvmdn/nba-free-agent-tracker.git"
  },
  "scripts": {
    "build": "npm run clean && babel server/src/ -d server/dist/",
    "build:watch": "npm run clean && babel server/src/ -d server/dist/ --watch",
    "clean": "rimraf server/dist/",
    "start": "npm run build && node server/dist .",
    "watch": "nodemon server/dist ."
  },
  "keywords": [],
  "author": "Kashav Madan <kshvmdn@gmail.com> (kshvmdn.me)",
  "license": "MIT",
  "babel": {
    "presets": [
      "stage-0",
      "es2015"
    ],
    "plugins": [
      "transform-runtime"
    ]
  },
  "devDependencies": {
    "babel-cli": "^6.10.1",
    "babel-plugin-transform-runtime": "^6.9.0",
    "babel-preset-es2015": "^6.9.0",
    "babel-preset-stage-0": "^6.5.0",
    "nodemon": "^1.9.2",
    "rimraf": "^2.5.2"
  },
  "dependencies": {
    "express": "^4.14.0",
    "morgan": "^1.7.0",
    "python-shell": "^0.4.0"
  }
}
