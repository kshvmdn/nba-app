{
  "name": "nba-free-agent-tracker",
  "version": "0.0.0",
  "description": "",
  "repository": {
    "type": "git",
    "url": "https://github.com/kshvmdn/nba-free-agent-tracker.git"
  },
  "scripts": {
    "build": "npm run clean && babel server/src/ -d server/lib/",
    "build:watch": "npm run clean && babel server/src/ -d server/lib/ --watch",
    "clean": "rimraf server/lib/",
    "start": "npm run build && node server/lib .",
    "watch": "nodemon server/lib ."
  },
  "keywords": [],
  "author": "Kashav Madan <kshvmdn@gmail.com> (kshvmdn.me)",
  "license": "MIT",
  "babel": {
    "presets": [
      "es2015"
    ]
  },
  "devDependencies": {
    "babel-cli": "^6.11.4",
    "babel-preset-es2015": "^6.9.0",
    "nodemon": "^1.10.0",
    "rimraf": "^2.5.4"
  },
  "dependencies": {
    "axios": "^0.13.1",
    "express": "^4.14.0",
    "moment": "^2.14.1",
    "morgan": "^1.7.0"
  }
}
